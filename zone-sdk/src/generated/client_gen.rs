// Generated by logos-lidl-gen from the `blockchain_module` LIDL contract — do not edit.
//
// Typed caller + event subscribers over logos_rust_sdk's lp_* consumer.

use logos_rust_sdk::{EventData, EventSubscription, LogosError, LogosModuleSDK, PluginProxy};

pub struct BlockchainModuleClient {
    proxy: PluginProxy,
}

impl BlockchainModuleClient {
    /// Client bound to the contract's own module name (`blockchain_module`) —
    /// the concrete-dependency pattern.
    pub fn new() -> Self {
        Self { proxy: LogosModuleSDK::new().plugin("blockchain_module") }
    }

    /// Bind the same typed surface to ANY provider chosen at runtime —
    /// the interface-dependency pattern: the contract names the shape,
    /// the caller names the module.
    pub fn bind(module_name: &str) -> Self {
        Self { proxy: LogosModuleSDK::new().plugin(module_name) }
    }

    pub fn start(&self, config_path: &str, deployment: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path), serde_json::Value::from(deployment)]);
        let value = self.proxy.call_json("start", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::start`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `startAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn start_async<F>(&self, config_path: &str, deployment: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path), serde_json::Value::from(deployment)]);
        self.proxy.call_json_async("start", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn stop(&self) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("stop", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::stop`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `stopAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn stop_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("stop", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn generate_user_config(&self, json_args: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(json_args)]);
        let value = self.proxy.call_json("generate_user_config", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::generate_user_config`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `generate_user_configAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn generate_user_config_async<F>(&self, json_args: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(json_args)]);
        self.proxy.call_json_async("generate_user_config", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn update_user_config(&self, user_config_path: &str, keystore_path: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path)]);
        let value = self.proxy.call_json("update_user_config", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::update_user_config`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `update_user_configAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn update_user_config_async<F>(&self, user_config_path: &str, keystore_path: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path)]);
        self.proxy.call_json_async("update_user_config", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn migrate_user_config(&self, output_path: &str, keystore_path: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(output_path), serde_json::Value::from(keystore_path)]);
        let value = self.proxy.call_json("migrate_user_config", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::migrate_user_config`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `migrate_user_configAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn migrate_user_config_async<F>(&self, output_path: &str, keystore_path: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(output_path), serde_json::Value::from(keystore_path)]);
        self.proxy.call_json_async("migrate_user_config", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn migrate_user_config_0_1_2(&self, new_config_path: &str, old_config_path: &str, keystore_path: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(new_config_path), serde_json::Value::from(old_config_path), serde_json::Value::from(keystore_path)]);
        let value = self.proxy.call_json("migrate_user_config_0_1_2", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::migrate_user_config_0_1_2`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `migrate_user_config_0_1_2Async`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn migrate_user_config_0_1_2_async<F>(&self, new_config_path: &str, old_config_path: &str, keystore_path: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(new_config_path), serde_json::Value::from(old_config_path), serde_json::Value::from(keystore_path)]);
        self.proxy.call_json_async("migrate_user_config_0_1_2", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn participate(&self, config_path: &str, keystore_path: &str, output_dir: &str, external_address: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(output_dir), serde_json::Value::from(external_address)]);
        let value = self.proxy.call_json("participate", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::participate`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `participateAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn participate_async<F>(&self, config_path: &str, keystore_path: &str, output_dir: &str, external_address: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(output_dir), serde_json::Value::from(external_address)]);
        self.proxy.call_json_async("participate", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn generate_key(&self, user_config_path: &str, keystore_path: &str, key_type: &str, key_title: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_type), serde_json::Value::from(key_title)]);
        let value = self.proxy.call_json("generate_key", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::generate_key`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `generate_keyAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn generate_key_async<F>(&self, user_config_path: &str, keystore_path: &str, key_type: &str, key_title: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_type), serde_json::Value::from(key_title)]);
        self.proxy.call_json_async("generate_key", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn add_key(&self, user_config_path: &str, keystore_path: &str, key_type: &str, key_hex: &str, key_title: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_type), serde_json::Value::from(key_hex), serde_json::Value::from(key_title)]);
        let value = self.proxy.call_json("add_key", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::add_key`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `add_keyAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn add_key_async<F>(&self, user_config_path: &str, keystore_path: &str, key_type: &str, key_hex: &str, key_title: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_type), serde_json::Value::from(key_hex), serde_json::Value::from(key_title)]);
        self.proxy.call_json_async("add_key", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn remove_key(&self, user_config_path: &str, keystore_path: &str, key_title: &str) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_title)]);
        let value = self.proxy.call_json("remove_key", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::remove_key`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `remove_keyAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn remove_key_async<F>(&self, user_config_path: &str, keystore_path: &str, key_title: &str, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(user_config_path), serde_json::Value::from(keystore_path), serde_json::Value::from(key_title)]);
        self.proxy.call_json_async("remove_key", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn get_peer_id(&self, config_path: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path)]);
        let value = self.proxy.call_json("get_peer_id", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_peer_id`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_peer_idAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_peer_id_async<F>(&self, config_path: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(config_path)]);
        self.proxy.call_json_async("get_peer_id", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn wallet_get_balance(&self, address_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(address_hex)]);
        let value = self.proxy.call_json("wallet_get_balance", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::wallet_get_balance`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_get_balanceAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_get_balance_async<F>(&self, address_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(address_hex)]);
        self.proxy.call_json_async("wallet_get_balance", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn wallet_transfer_funds(&self, change_public_key: &str, sender_addresses: &serde_json::Value, recipient_address: &str, amount: &str, optional_tip_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(change_public_key), sender_addresses.clone(), serde_json::Value::from(recipient_address), serde_json::Value::from(amount), serde_json::Value::from(optional_tip_hex)]);
        let value = self.proxy.call_json("wallet_transfer_funds", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::wallet_transfer_funds`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_transfer_fundsAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_transfer_funds_async<F>(&self, change_public_key: &str, sender_addresses: &serde_json::Value, recipient_address: &str, amount: &str, optional_tip_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(change_public_key), sender_addresses.clone(), serde_json::Value::from(recipient_address), serde_json::Value::from(amount), serde_json::Value::from(optional_tip_hex)]);
        self.proxy.call_json_async("wallet_transfer_funds", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn wallet_get_known_addresses(&self) -> Result<serde_json::Value, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("wallet_get_known_addresses", &args)?;
        Ok(value)
    }

    /// Async twin of [`Self::wallet_get_known_addresses`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_get_known_addressesAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_get_known_addresses_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<serde_json::Value, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("wallet_get_known_addresses", &args, move |result| {
            callback(result.and_then(|value| Ok(value)));
        });
    }

    pub fn wallet_get_notes(&self, wallet_address_hex: &str, optional_tip_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(wallet_address_hex), serde_json::Value::from(optional_tip_hex)]);
        let value = self.proxy.call_json("wallet_get_notes", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::wallet_get_notes`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_get_notesAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_get_notes_async<F>(&self, wallet_address_hex: &str, optional_tip_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(wallet_address_hex), serde_json::Value::from(optional_tip_hex)]);
        self.proxy.call_json_async("wallet_get_notes", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn leader_claim(&self) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("leader_claim", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::leader_claim`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `leader_claimAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn leader_claim_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("leader_claim", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn wallet_get_claimable_vouchers(&self) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("wallet_get_claimable_vouchers", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::wallet_get_claimable_vouchers`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_get_claimable_vouchersAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_get_claimable_vouchers_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("wallet_get_claimable_vouchers", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn wallet_fund_tx(&self, request_json: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(request_json)]);
        let value = self.proxy.call_json("wallet_fund_tx", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::wallet_fund_tx`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `wallet_fund_txAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn wallet_fund_tx_async<F>(&self, request_json: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(request_json)]);
        self.proxy.call_json_async("wallet_fund_tx", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn submit_signed_transaction(&self, signed_tx_json: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(signed_tx_json)]);
        let value = self.proxy.call_json("submit_signed_transaction", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::submit_signed_transaction`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `submit_signed_transactionAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn submit_signed_transaction_async<F>(&self, signed_tx_json: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(signed_tx_json)]);
        self.proxy.call_json_async("submit_signed_transaction", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn channel_deposit(&self, channel_id_hex: &str, funding_public_key_hex: &str, amount: &str, metadata_hex: &str, optional_tip_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex), serde_json::Value::from(funding_public_key_hex), serde_json::Value::from(amount), serde_json::Value::from(metadata_hex), serde_json::Value::from(optional_tip_hex)]);
        let value = self.proxy.call_json("channel_deposit", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::channel_deposit`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `channel_depositAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn channel_deposit_async<F>(&self, channel_id_hex: &str, funding_public_key_hex: &str, amount: &str, metadata_hex: &str, optional_tip_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex), serde_json::Value::from(funding_public_key_hex), serde_json::Value::from(amount), serde_json::Value::from(metadata_hex), serde_json::Value::from(optional_tip_hex)]);
        self.proxy.call_json_async("channel_deposit", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn channel_deposit_with_notes(&self, channel_id_hex: &str, input_note_id_hexes: &serde_json::Value, metadata_hex: &str, change_public_key_hex: &str, funding_public_key_hexes: &serde_json::Value, max_tx_fee: &str, optional_tip_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex), input_note_id_hexes.clone(), serde_json::Value::from(metadata_hex), serde_json::Value::from(change_public_key_hex), funding_public_key_hexes.clone(), serde_json::Value::from(max_tx_fee), serde_json::Value::from(optional_tip_hex)]);
        let value = self.proxy.call_json("channel_deposit_with_notes", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::channel_deposit_with_notes`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `channel_deposit_with_notesAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn channel_deposit_with_notes_async<F>(&self, channel_id_hex: &str, input_note_id_hexes: &serde_json::Value, metadata_hex: &str, change_public_key_hex: &str, funding_public_key_hexes: &serde_json::Value, max_tx_fee: &str, optional_tip_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex), input_note_id_hexes.clone(), serde_json::Value::from(metadata_hex), serde_json::Value::from(change_public_key_hex), funding_public_key_hexes.clone(), serde_json::Value::from(max_tx_fee), serde_json::Value::from(optional_tip_hex)]);
        self.proxy.call_json_async("channel_deposit_with_notes", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_channel_state(&self, channel_id_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex)]);
        let value = self.proxy.call_json("get_channel_state", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_channel_state`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_channel_stateAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_channel_state_async<F>(&self, channel_id_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(channel_id_hex)]);
        self.proxy.call_json_async("get_channel_state", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn blend_join_as_core_node(&self, locator: &str, locked_note_id_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(locator), serde_json::Value::from(locked_note_id_hex)]);
        let value = self.proxy.call_json("blend_join_as_core_node", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::blend_join_as_core_node`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `blend_join_as_core_nodeAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn blend_join_as_core_node_async<F>(&self, locator: &str, locked_note_id_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(locator), serde_json::Value::from(locked_note_id_hex)]);
        self.proxy.call_json_async("blend_join_as_core_node", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_block(&self, header_id_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(header_id_hex)]);
        let value = self.proxy.call_json("get_block", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_block`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_blockAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_block_async<F>(&self, header_id_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(header_id_hex)]);
        self.proxy.call_json_async("get_block", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_blocks(&self, from_slot: i64, to_slot: i64) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(from_slot), serde_json::Value::from(to_slot)]);
        let value = self.proxy.call_json("get_blocks", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_blocks`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_blocksAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_blocks_async<F>(&self, from_slot: i64, to_slot: i64, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(from_slot), serde_json::Value::from(to_slot)]);
        self.proxy.call_json_async("get_blocks", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_transaction(&self, tx_hash_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(tx_hash_hex)]);
        let value = self.proxy.call_json("get_transaction", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_transaction`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_transactionAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_transaction_async<F>(&self, tx_hash_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(tx_hash_hex)]);
        self.proxy.call_json_async("get_transaction", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_cryptarchia_info(&self) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("get_cryptarchia_info", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_cryptarchia_info`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_cryptarchia_infoAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_cryptarchia_info_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("get_cryptarchia_info", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_block_events(&self, header_id_hex: &str) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(header_id_hex)]);
        let value = self.proxy.call_json("get_block_events", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_block_events`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_block_eventsAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_block_events_async<F>(&self, header_id_hex: &str, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![serde_json::Value::from(header_id_hex)]);
        self.proxy.call_json_async("get_block_events", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    pub fn get_time_info(&self) -> Result<String, LogosError> {
        let args = serde_json::Value::Array(vec![]);
        let value = self.proxy.call_json("get_time_info", &args)?;
        Ok(value.as_str().unwrap_or_default().to_string())
    }

    /// Async twin of [`Self::get_time_info`]: fire the call and receive the typed
    /// result in `callback` once it lands — the Rust analog of the C++
    /// client's `get_time_infoAsync`. The callback runs from the protocol
    /// completion path (the module's Qt event loop), so it fires after
    /// the current method returns, never inline.
    pub fn get_time_info_async<F>(&self, callback: F)
    where
        F: FnOnce(Result<String, LogosError>) + Send + 'static,
    {
        let args = serde_json::Value::Array(vec![]);
        self.proxy.call_json_async("get_time_info", &args, move |result| {
            callback(result.and_then(|value| Ok(value.as_str().unwrap_or_default().to_string())));
        });
    }

    /// Subscribe to the `newBlock` event. Payload arrives as a JSON array of [blockJson: tstr];
    /// decode each received item with [`Self::decode_new_block`]. The returned
    /// subscription owns its client share — move it into a listener
    /// thread and iterate it; drop it to unsubscribe.
    pub fn on_new_block(&mut self) -> Result<EventSubscription, LogosError> {
        self.proxy.on("newBlock")
    }

    /// Decode a received `newBlock` payload into its typed form.
    /// Returns None if the payload doesn't match the contract.
    pub fn decode_new_block(ev: &EventData) -> Option<NewBlockEvent> {
        let arr = ev.data.as_array()?;
        if arr.len() < 1 { return None; }
        Some(NewBlockEvent {
            block_json: arr[0].as_str()?.to_string(),
        })
    }

    /// Subscribe to the `processedBlock` event. Payload arrives as a JSON array of [eventJson: tstr];
    /// decode each received item with [`Self::decode_processed_block`]. The returned
    /// subscription owns its client share — move it into a listener
    /// thread and iterate it; drop it to unsubscribe.
    pub fn on_processed_block(&mut self) -> Result<EventSubscription, LogosError> {
        self.proxy.on("processedBlock")
    }

    /// Decode a received `processedBlock` payload into its typed form.
    /// Returns None if the payload doesn't match the contract.
    pub fn decode_processed_block(ev: &EventData) -> Option<ProcessedBlockEvent> {
        let arr = ev.data.as_array()?;
        if arr.len() < 1 { return None; }
        Some(ProcessedBlockEvent {
            event_json: arr[0].as_str()?.to_string(),
        })
    }

    /// Subscribe to the `libBlock` event. Payload arrives as a JSON array of [blockInfoJson: tstr];
    /// decode each received item with [`Self::decode_lib_block`]. The returned
    /// subscription owns its client share — move it into a listener
    /// thread and iterate it; drop it to unsubscribe.
    pub fn on_lib_block(&mut self) -> Result<EventSubscription, LogosError> {
        self.proxy.on("libBlock")
    }

    /// Decode a received `libBlock` payload into its typed form.
    /// Returns None if the payload doesn't match the contract.
    pub fn decode_lib_block(ev: &EventData) -> Option<LibBlockEvent> {
        let arr = ev.data.as_array()?;
        if arr.len() < 1 { return None; }
        Some(LibBlockEvent {
            block_info_json: arr[0].as_str()?.to_string(),
        })
    }
}

/// Typed payload of the `newBlock` event.
#[derive(Debug, Clone)]
pub struct NewBlockEvent {
    pub block_json: String,
}

/// Typed payload of the `processedBlock` event.
#[derive(Debug, Clone)]
pub struct ProcessedBlockEvent {
    pub event_json: String,
}

/// Typed payload of the `libBlock` event.
#[derive(Debug, Clone)]
pub struct LibBlockEvent {
    pub block_info_json: String,
}

impl Default for BlockchainModuleClient {
    fn default() -> Self {
        Self::new()
    }
}
